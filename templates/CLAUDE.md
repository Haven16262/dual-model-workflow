## 双模型工作流

本项目使用双模型协作模式。**每次 session 开始时必须按顺序执行：**

1. 读取 `WORKFLOW.md` — 了解角色职责和切换规则
2. 读取 `context.md` — 优先看顶部 `## 当前状态` 区块，再看最新历史记录
3. 复述确认：「我是[全局者/工作者]，当前任务是：[任务摘要]」，然后开始工作

当前角色由用户在 session 开始时告知（直接说明，或通过「全局者/工作者修改了内容，查看 context 文件」触发）。
